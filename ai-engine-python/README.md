# ai-engine-python

Python AI engine (FastAPI + agent orchestration).

Phase 1 scope:
- `orchestrator/`: FastAPI app exposing `/v1/plan`
- `agents/`: Intent/Budget/Itinerary agents
- `memory/`: run artifact persistence (Postgres)
- `rag/` and `mcp/`: placeholders for later phases

